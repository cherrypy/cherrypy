# For compatibility. Consider removing in CP4 final.
from cherrypy.lib.dispatch.base import (Dispatcher,
                                        MethodDispatcher,
                                        RoutesDispatcher,
                                        VirtualHost,
                                        XMLRPCDispatcher)
